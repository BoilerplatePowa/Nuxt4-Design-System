export const timelinePartMap = {
    start: 'timeline-start',
    middle: 'timeline-middle',
    end: 'timeline-end',
} as const

export const timelineModifierMap = {
    'snap-icon': 'timeline-snap-icon',
    box: 'timeline-box',
    compact: 'timeline-compact',
} as const

export const timelineDirectionMap = {
    horizontal: 'timeline-horizontal',
    vertical: 'timeline-vertical',
} as const

export const timelineBaseClass = 'timeline' as const
